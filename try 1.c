#include <stdio.h>
#include <csbootcamp.h>
#include <string.h>
#include <ctype.h>
#include <stdlib.h>

//4biti = nibble (halfbyte);

typedef struct Masina
{
 char* marca;
 char *model;
 char tokenMasina[11];
 int pretAchizitie;
 int pretVanzare;
}Masina;

int main(int argc, char *argv[])
{

char *input;
    int v[2700];

FILE *inputCerinte = fopen(argv[1], "r");
    if(file==NULL)perror("Error opening file");
    return 1;}
    FILE *inputDate = fopen(argv[2], "r");
     if(file==NULL)perror("Error opening file");
    return 1;}
   

    int suma=0;
    while(reading)
        { suma=0;
         scanf(input,"%s)

            for(int i =0; i<strlen(input);i++)
            {
                suma=suma+int(toupper(char *input[i]));
            }
            v[suma]++;
        }
        
}
